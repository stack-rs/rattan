[project]
name = "scripts"
version = "0.1.0"
description = "Some utility scripts for Rattan."
requires-python = ">=3.11"
dependencies = [
  "pcapng>=0.1.25",
  "python-pcapng>=2.1.1",
  "scapy>=2.6.1",
]
